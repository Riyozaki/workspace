#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
if [[ ! -x "$ROOT/.venv/bin/documentctl" ]]; then
  echo "documentctl is not installed. Run scripts/bootstrap-documents.sh first." >&2
  exit 1
fi
exec "$ROOT/.venv/bin/documentctl" "$@"
